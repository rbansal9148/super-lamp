# Non-secret configuration. Safe to commit. Split by tools/split_env.py.
# Add new non-secret vars here; secrets belong in .env.


# Non-secret config flags — must stay in config.env even with secret-shaped suffix
ENABLE_PSTREAM_API=false
DISABLE_LEGACY_API=true
PUBLIC_METRICS_API=True
REDIS_CACHE_TTL_API=3600

# Plain config
LOG_LEVEL=info
PORT=3000
