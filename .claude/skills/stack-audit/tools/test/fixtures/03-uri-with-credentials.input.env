# Values that are credentialed URIs must be treated as secret even when
# the KEY name itself is generic (no PASSWORD/TOKEN/SECRET marker).
SOME_GENERIC_URI=postgresql://app:pa55w0rd@db:5432/app
ANOTHER_GENERIC=https://service.example.com/health
REDIS_URI=redis://redis:6379
