..*

notification.txt
data
*.log
*.db

!data/searxng/settings.yml

*.env
!**/config.env

# Scheduler runtime state (loop/scheduled-task lock)
/.claude/scheduled_tasks.lock

# Runtime-cloned nested repos (not tracked here)
/Indexers/
/apps/calibre-web-automated/heroofhearts/
/apps/invidious/
.serena/

__pycache__/
*.pyc

# Decommissioned Docker stack — removed from git (was the plaintext-secret leak source).
# Data lives in /opt/docker/data (gitignored); k8s manifests live in gitops/.
/apps/
